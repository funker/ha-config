---

# components/button.yaml

button:
  - platform: restart
    name: "Restart"

  - platform: safe_mode
    id: button_safe_mode
    name: "Safe Mode Boot"

  - platform: factory_reset
    id: factory_reset_btn
    name: "Factory Reset"

