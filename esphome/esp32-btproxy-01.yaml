---

# esp32-btproxy-01.yaml

substitutions:
  # Default substitutions
  config_tested_date: "2006-06-03"
  devicename: "esp32-btproxy-01"
  friendly_devicename: "Bluetooth Proxy Jan"
  room: "Jan"
  comment: "ESP32 Bluetooth Proxy 01"
  project_name: "Espressif.ESP32"
  project_version: "1.0"

packages:
  default_package: !include common/base-device.yaml
  device_type: !include device_types/boards/esp32-generic.yaml
  role: !include roles/esp32-btproxy.yaml
  
  # Online version
  # esphome.bluetooth-proxy: github://esphome/bluetooth-proxies/esp32-generic/esp32-generic.yaml@main

