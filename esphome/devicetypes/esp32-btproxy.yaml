# devicetypes/esp32-btproxy.yaml

esp32_ble_tracker:
  scan_parameters:
    active: true

bluetooth_proxy:
  active: true

button:
  - platform: safe_mode
    id: button_safe_mode
    name: "Safe Mode Boot"

  - platform: factory_reset
    id: factory_reset_btn
    name: "Factory Reset"