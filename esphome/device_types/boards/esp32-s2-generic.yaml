---

# device_types/boards/esp32-s2-generic.yaml

packages:
  base_network_esp: !include ../../common/base-network-esp.yaml

esp32:
  board: lolin_s2_mini
  variant: ESP32S2
  framework:
    type: arduino

