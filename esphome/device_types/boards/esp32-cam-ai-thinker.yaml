---

# device_types/boards/esp32-cam-ai-thinker.yaml

packages:
  base_network_esp: !include ../../common/base-network-esp.yaml

esp32:
  board: esp32cam
  framework:
    type: arduino
    advanced:
      minimum_chip_revision: "3.0"

psram:
  mode: quad